services:
  viewra:
    build:
      context: .
      dockerfile: Dockerfile
    image: viewra:latest
    container_name: viewra
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NODE_ENV: production
    restart: unless-stopped
